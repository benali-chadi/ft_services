apiVersion: v1
kind: Service
metadata:
  name: my-mysql
  labels:
    app: my-mysql
spec:
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: 3306
    name: mysql
  selector:
    app: my-mysql
  clusterIP: 10.107.84.226
---
  apiVersion: apps/v1
  kind: Deployment
  metadata:
    name: my-mysql
  spec:
    replicas: 1
    selector:
      matchLabels:
        app: my-mysql
    template:
      metadata:
        labels:
          app: my-mysql
      spec:
        containers:
        - name: my-mysql
          image: my-mysql
          imagePullPolicy: Never
          ports:
            - containerPort: 3306


# 10.107.84.226
# kubectl apply -f <path-to-yaml-file>